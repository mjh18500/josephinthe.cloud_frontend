module.exports = {
  darkMode: "class", // so you can toggle by adding `.dark` on <html> or <body>
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: { extend: {} },
  plugins: [],
}
